### Tested on Ubuntu 22.04.4 LTS

Quickly set up the Timpi Collector on your Linux system with this one-command installation. This script will download and install version 0.9.0 of the Timpi Collector and then automatically upgrade it to version 0.9.1, overwriting any previous files.

**Command to run:**
```bash
sudo apt-get install -y dos2unix && curl -o Automated_collector_script.sh https://raw.githubusercontent.com/Timpi-official/Nodes/main/Collector/Automated_collector_script.sh && dos2unix Automated_collector_script.sh && bash Automated_collector_script.sh


**Here's what happens:**
1. **Prepares your system**: Installs `dos2unix` to ensure script compatibility.
2. **Downloads and runs the setup script**: Fetches the latest setup script and executes it to install and configure the Timpi Collector.
3. **Initial installation**: Installs version 0.9.0 of the Timpi Collector.
4. **Automatic upgrade**: Upgrades the installation to version 0.9.1, seamlessly overwriting previous installation files to ensure you're up-to-date.

**After installation:**
- Open your browser and visit `http://localhost:5015/collector` to access the Timpi Collector interface.

**Collector Commands:**
- **View live status**: `sudo journalctl -xeu collector -f`
- **Start the collector**: `sudo systemctl start collector`
- **Stop the collector**: `sudo systemctl stop collector`
- **Restart the collector**: `sudo systemctl restart collector`
- **Restart the UI**: `sudo systemctl restart collector_ui`
- *Optional*: Check the status of the collector and UI using `sudo systemctl status collector` and `sudo systemctl status collector_ui`.

Just copy, paste, and press Enter


**Here´s a removal command:**
```bash
sudo systemctl stop collector collector_ui || true; sudo systemctl disable collector collector_ui || true; sudo rm -rf /opt/timpi; sudo apt-get purge -y dos2unix; sudo apt-get autoremove -y; echo "Collector and dos2unix have been removed successfully."
```

 (```)
